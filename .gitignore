# _build/ — билд-система (Python-сборщики + контент-JSON + HANDOFF.md).
# Закоммичена в репозиторий для сохранности, но НЕ публикуется:
# Jekyll (GitHub Pages) исключает папки с префиксом "_" из собранного сайта.
